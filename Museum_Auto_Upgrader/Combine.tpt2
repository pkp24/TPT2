:name D0S.Museum v2.0.0:Combine

:global int MaxCraftLevel

gotoif(end, MaxCraftLevel <= tier("inventory",0))
combine(0)
gotoif(end, MaxCraftLevel <= tier("inventory",0))
combine(0)
combine(0)
combine(0)
combine(0)

end:
wait(0.0)
